# Authentication Secret - Required for Better Auth
# Generate a secure random string (at least 32 characters)
# For production, use a strong, unique value
AUTH_SECRET=your-long-secure-random-string

# Cloudflare D1 is configured through wrangler.jsonc
# No need to set DATABASE_URL as D1 is bound to your application
